# Environment variables for your Waaseyaa application.
#
# This file is committed to version control as a template.
# Copy to .env.local for personal overrides (never committed).
#
# Precedence: process env > .env.local > .env

# ── Application ────────────────────────────────────────────────────────────────
APP_NAME=Waaseyaa
APP_ENV=local
APP_DEBUG=true
# APP_HOST=127.0.0.1  # Uncomment to restrict the dev server to localhost only
APP_URL=http://localhost:8080

# Minimum log level: debug | info | notice | warning | error | critical | alert | emergency
# LOG_LEVEL=warning

# ── Database ───────────────────────────────────────────────────────────────────
WAASEYAA_DB=./storage/waaseyaa.sqlite

# ── Authentication ─────────────────────────────────────────────────────────────
# Auto-generated by post-create-setup — do not change unless rotating the secret.
WAASEYAA_JWT_SECRET=

# Dev-only: auto-authenticate as admin on the built-in PHP server.
# Only works when APP_ENV=local and using `composer run dev`.
WAASEYAA_DEV_FALLBACK_ACCOUNT=true

# ── MCP agent (POST /mcp) ──────────────────────────────────────────────────────
# Bearer token for the dedicated MCP agent service account (mcp-agent@fnprocure.ca,
# created via `vendor/bin/waaseyaa mcp:agent-account`). Generate a long random
# secret (e.g. `openssl rand -hex 32`); never commit the real value. Unset = the
# /mcp endpoint rejects every request (fail closed).
WAASEYAA_MCP_AGENT_TOKEN=

# ── CORS ───────────────────────────────────────────────────────────────────────
# Allowed origin for the admin SPA. Defaults to http://localhost:3000.
# WAASEYAA_CORS_ORIGIN=http://localhost:3000

# ── Translation (M-006) ────────────────────────────────────────────────────────
# Read content entities through the active-language translation resolver.
# Default: false (read the base entity row — legacy behaviour). Set to true to
# opt in to per-language reads with fallback chain resolution.
# WAASEYAA_TRANSLATION_READ_ACTIVE_LANGUAGE=false
